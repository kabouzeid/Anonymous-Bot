# Random Coffee Bot for JetBrains Space

## Requirements
Specify a Postgres database connection via environment vars, for example:
```bash
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=""
DB_NAME=postgres
```
